from aladdin.codable import Codable

class StreamDataSource(Codable):
    """
    Used to determend if an API call should be created, or if we should listen to a stream.
    """
    def do_not_know_the_interface_yet(self):
        pass